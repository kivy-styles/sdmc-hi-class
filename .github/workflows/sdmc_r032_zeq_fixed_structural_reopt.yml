name: SDMC r032 equality-fixed structural reopt

on:
  push:
    branches:
      - sdmc-r032-zeq-fixed-structural-reopt
  workflow_dispatch:

jobs:
  zeq-fixed-structural:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3 python3-pip git
          python3 -m pip install --upgrade pip
          python3 -m pip install numpy scipy pandas cobaya

      - name: Apply SDMC patches and build
        run: |
          python3 sdmc/apply_sdmc_tracker_patch.py
          python3 sdmc/apply_sdmc_full_background_patch.py
          python3 sdmc/apply_sdmc_v2_noslip_patch.py
          python3 sdmc/apply_sdmc_v2_smooth_noslip_patch.py
          python3 sdmc/apply_sdmc_split_sector_patch.py
          python3 sdmc/fix_sdmc_split_sector_background.py
          python3 sdmc/fix_parameterized_alphaM_refresh.py
          python3 sdmc/apply_sdmc_v3_native_noslip_patch.py
          python3 sdmc/run_sdmc_cs2_term_audit.py instrument
          python3 sdmc/install_sdmc_v3_independent_kinetic.py
          make -j2 class

      - name: Install Planck native likelihood data
        run: |
          python3 - <<'PY'
          from cobaya.likelihoods.planck_2018_highl_plik.TTTEEE_lite_native import TTTEEE_lite_native
          from cobaya.likelihoods.planck_2018_lowl.TT import TT
          from cobaya.likelihoods.planck_2018_lowl.EE import EE
          from cobaya.likelihoods.planck_2018_lensing import native as LensingNative
          for cls in (TTTEEE_lite_native,TT,EE,LensingNative):
              if not cls.install(path='planck_packages',no_progress_bars=True):
                  raise SystemExit(f'install failed {cls}')
          PY

      - name: Install SN data
        run: |
          rm -rf sn_data
          git clone --depth 1 --branch v1.8 https://github.com/CobayaSampler/sn_data.git sn_data

      - name: Run equality-fixed structural re-optimization
        run: python3 sdmc/run_r032_zeq_fixed_structural_reopt.py

      - name: Print summary
        if: always()
        run: |
          test ! -f output/zeq_fixed_structural/zeq_fixed_structural_summary.json || cat output/zeq_fixed_structural/zeq_fixed_structural_summary.json
          test ! -f output/zeq_fixed_structural/zeq_fixed_structural_reopt.csv || head -8 output/zeq_fixed_structural/zeq_fixed_structural_reopt.csv

      - name: Preserve equality-fixed structural scan
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sdmc-r032-zeq-fixed-structural-reopt
          path: |
            output/zeq_fixed_structural/*.csv
            output/zeq_fixed_structural/*.json
          if-no-files-found: warn
