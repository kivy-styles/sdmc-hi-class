name: SDMC r032 deep low-H direct SN closure screen

on:
  push:
    branches:
      - sdmc-r032-snclosure-deep-lowh-screen
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snclosure:
    runs-on: ubuntu-latest
    timeout-minutes: 150
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3 python3-pip git
          python3 -m pip install --upgrade pip
          python3 -m pip install numpy scipy pandas cobaya

      - name: Apply SDMC patches and build
        run: |
          python3 sdmc/apply_sdmc_tracker_patch.py
          python3 sdmc/apply_sdmc_full_background_patch.py
          python3 sdmc/apply_sdmc_v2_noslip_patch.py
          python3 sdmc/apply_sdmc_v2_smooth_noslip_patch.py
          python3 sdmc/apply_sdmc_split_sector_patch.py
          python3 sdmc/fix_sdmc_split_sector_background.py
          python3 sdmc/fix_parameterized_alphaM_refresh.py
          python3 sdmc/apply_sdmc_v3_native_noslip_patch.py
          python3 sdmc/run_sdmc_cs2_term_audit.py instrument
          python3 sdmc/install_sdmc_v3_independent_kinetic.py
          make -j2 class

      - name: Install Planck native-lite likelihood data
        run: |
          ok=0
          for attempt in 1 2 3 4
          do
            rm -rf planck_packages
            if python3 - <<'PY'
          from cobaya.likelihoods.planck_2018_highl_plik.TTTEEE_lite_native import TTTEEE_lite_native
          from cobaya.likelihoods.planck_2018_lowl.TT import TT
          from cobaya.likelihoods.planck_2018_lowl.EE import EE
          from cobaya.likelihoods.planck_2018_lensing import native as LensingNative
          for cls in (TTTEEE_lite_native,TT,EE,LensingNative):
              if not cls.install(path='planck_packages',no_progress_bars=True):
                  raise SystemExit(2)
          print('PLANCK_LITE_READY')
          PY
            then ok=1; break; fi
            sleep $((attempt*6))
          done
          test "$ok" -eq 1

      - name: Install SN and DESI BAO covariance products
        run: |
          rm -rf sn_data bao_data
          git clone --depth 1 --branch v1.8 https://github.com/CobayaSampler/sn_data.git sn_data
          git clone --depth 1 https://github.com/CobayaSampler/bao_data.git bao_data

      - name: Run deep low-H direct closure screen
        run: python3 sdmc/run_r032_snclosure_deep_lowh_screen.py

      - name: Print closure summary
        if: always()
        run: |
          cat output/snclosure_deep_lowh/snclosure_summary.json || true
          echo "--- TOP PLANCK SHORTLIST ---"
          head -16 output/snclosure_deep_lowh/snclosure_planck_shortlist.csv || true

      - name: Preserve closure screen
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sdmc-r032-snclosure-deep-lowh
          path: |
            output/snclosure_deep_lowh/*.csv
            output/snclosure_deep_lowh/*.json
          if-no-files-found: warn
